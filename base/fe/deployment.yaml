apiVersion: apps/v1
kind: Deployment
metadata:
  name: attendance-checker-fe-deployment
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: attendance-checker-fe-prod
  replicas: 1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: attendance-checker-fe-prod
    spec:
      containers:
      - image: scgskku/attendance-checker-fe-prod
        imagePullPolicy: Always
        name: attendance-checker-fe-prod
        ports:
        - containerPort: 3000
